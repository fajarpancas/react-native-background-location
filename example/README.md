# Example

## Setup

```bash
# Init React Native project (run from example/ directory)
npx @react-native-community/cli init Example --directory .

# Install dependencies
npm install

# iOS only
npx pod-install

# Start
npx react-native start
```

Then press `i` for iOS or `a` for Android.
